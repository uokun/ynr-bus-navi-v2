# E2E Test Suite Specification & Execution Report (TEST_READY)

## 1. 概要 (Overview)
横浜市営バス乗り継ぎ案内・リアルタイム運行情報Webアプリケーション（`transporter`）向けに、Node.js環境で完全スタンドアロンで高速（<100ms）かつ高信頼に動作する4階層（Tier 1〜Tier 4）の自動E2Eテストスイートおよびテストランナーを構築しました。

外部npmパッケージに依存せず、純粋なNode.js（ES Modules）とインメモリDOM/ブラウザシミュレータ（`tests/test-harness.js`）により、ブラウザレスで即座にCI/CDおよびローカル検証が可能です。

---

## 2. テストスイート構成 & カバレッジ実績

| スイート | ファイル | 対象スコープ | 要件基準 | 実装テスト数 | 合格数 | 合格率 |
| :--- | :--- | :--- | :---: | :---: | :---: | :---: |
| **Tier 1** | `tests/tier1-feature-tests.js` | 全機能単体・正常系カバレッジ (F1〜F10) | $\ge 50$ (各$\ge 5$) | **53** | **53** | **100.0%** |
| **Tier 2** | `tests/tier2-boundary-tests.js` | 境界値・極限値・異常系・障害耐性 (B1〜B10) | $\ge 50$ (各$\ge 5$) | **54** | **54** | **100.0%** |
| **Tier 3** | `tests/tier3-combination-tests.js` | 複数機能・状態遷移の直交組み合わせ | $\ge 15$ | **16** | **16** | **100.0%** |
| **Tier 4** | `tests/tier4-scenario-tests.js` | 実世界通勤・行楽・遅延回復フルシナリオ | $\ge 8$ | **9** | **9** | **100.0%** |
| **合計** | **全スイート** | **全受入基準（AC-1〜AC-8）完全検証** | $\ge 123$ | **132** | **132** | **100.0%** |

---

## 3. テスト実行方法 (Execution Guide)

### 全テストの一括実行
```bash
node tests/e2e-runner.js
```

### 特定Tierのみの個別実行
```bash
# Tier 1（機能単体テスト）のみ実行
node tests/e2e-runner.js --tier=1

# Tier 2（境界値・異常系テスト）のみ実行
node tests/e2e-runner.js --tier=2

# Tier 3（組み合わせテスト）のみ実行
node tests/e2e-runner.js --tier=3

# Tier 4（実世界シナリオテスト）のみ実行
node tests/e2e-runner.js --tier=4
```

### フィルタリング & 詳細ログ出力
```bash
# 特定のテストIDやキーワードで絞り込み
node tests/e2e-runner.js --grep=T4

# 詳細ログ（スタックトレース等）を有効化
node tests/e2e-runner.js --verbose
```

### 終了コード（Exit Code）規約
- 全テスト合格時: `0`
- 1件以上の失敗時: `1`

---

## 4. 機能インベントリとテストマッピング詳細

### Tier 1: 機能カバレッジ（53テスト）
- **F1: PWA Shell & 静的アプリ起動 (6 tests)**: `manifest.json` スキーマ検証、`sw.js` サービスワーカーイベント（install/activate/fetch）、HTML5セマンティック構造（header/main/nav/footer）、viewportメタタグ設定、PWAテーマカラー、主要DOMマウント要素の存在確認。
- **F2: 横浜市営バスデザイン & ダークモード (5 tests)**: 横浜市営バスブルー（`#004098`）CSS変数、ライトテーマパレット、ダークテーマ（`[data-theme="dark"]`）変数、DOMテーマ切替属性操作、`localStorage` 永続化。
- **F3: 往復方向切替 (5 tests)**: 初期往路（洋光台北口→上大岡→古泉）、反転トグルでの復路（古泉→上大岡→洋光台北口）切替、ヘッダー表示文言更新、1区・2区系統設定スワップ、連続反転での整合性保持。
- **F4: 単体区間・停留所ビュー (5 tests)**: 乗り継ぎ/洋光台/上大岡/古泉タブ描画、タブ切替アクティブ状態制御、停留所別発車一覧描画、発車カウントダウン（「あとX分」）算出、乗り継ぎビュー復帰。
- **F5: 乗り継ぎ計算エンジン (6 tests)**: 111系統→133系統ハッピーパス計算、標準バッファ（5分）適用判定（$T_{dep2} \ge T_{arr1} + Buffer$）、最先着推奨便選定、次発候補2〜3便抽出、乗換待ち時間（$Wait = T_{dep2} - T_{arr1}$）計算、復路乗り継ぎ計算。
- **F6: 系統・行先フィルタリング (5 tests)**: 111系統絞り込み、133系統絞り込み、64系統絞り込み、行先（上大岡駅前/根岸駅前/磯子駅前）絞り込み、「全て」リセット。
- **F7: 設定モーダル & ストレージ永続化 (5 tests)**: 設定モーダル開閉DOM制御、カスタムAPIキー保存、乗り換えバッファ設定保存、初期フォーム読み込み、初期値リセット動作。
- **F8: ポーリングタイマー & 手動更新 (5 tests)**: 30秒ポーリング初期化、タイマーカウントダウン減算、手動更新ボタン即時フェッチ、最終更新時刻フォーマット（HH:MM:SS）、自動更新ON/OFFトグル。
- **F9: ODPT APIフォールバック & オフライン耐性 (6 tests)**: `odpt:BusstopPole` モック検証、`odpt:BusRoutePattern` モック検証、平日/土曜/休日ダイヤモック、fetchエラー時のモック自動フォールバック、`odpt:Bus` 遅延秒数スキーマ、オフラインUIフォールバック。
- **F10: クレジット表記 & メタデータ (5 tests)**: 「データ提供: 公共交通オープンデータ協議会」クレジット文言、`dc:date` データ生成日時、事業者ID `odpt.Operator:YokohamaMunicipal`、フッタークレジット常時表示、停留所・系統の公式名称一致。

### Tier 2: 境界値・極限値・異常系カバレッジ（54テスト）
- **B1: PWA Shell 境界 (5 tests)**: オフライン時のキャッシュフォールバック、不正なマニフェストJSONフォールバック、アイコン欠落時のSVGフォールバック、CacheStorage QuotaExceeded例外耐性、オフラインリロード耐性。
- **B2: テーマ表示境界 (5 tests)**: 不正テーマ文字列の安全なデフォルト化、100回連続トグル負荷耐性、`prefers-color-scheme` 連動、未設定属性耐性、コントラスト比検証。
- **B3: 方向切替境界 (5 tests)**: 高速ダブルクリック耐性、系統フィルター適用中の方向切替、0便該当時の空状態表示、カスタムバッファ保持、不正方向パラメータのサニタイズ。
- **B4: 時刻表境界 (6 tests)**: 終バス後（23:55）の「本日の運行は終了しました」表示、始発前（04:30）の始発便特定、24時台（24:15）深夜便のソート順、空配列レンダリング、0秒発車（まもなく発車）表示、過去発車便の除外。
- **B5: 乗り継ぎバッファ・遅延境界 (6 tests)**: 0分バッファ（即座乗り換え）、負のバッファ入力クランプ、120分極大バッファ上限クランプ、第1区間+20分大幅遅延による接続便自動追従・次発繰り上げ、第2区間+30分遅延時の到着予測更新、運休便（`isCancelled: true`）の自動除外。
- **B6: 祝日・ダイヤ判定境界 (6 tests)**: 国民の祝日（憲法記念日等）判定、振替休日判定（日曜祝日時の翌平日）、ハッピーマンデー動的算出（成人の日等）、天文学的春分/秋分の日計算、年末年始特別ダイヤ（12/29〜1/3）の休日扱い、うるう年（2/29）処理。
- **B7: フィルター境界 (5 tests)**: 該当便0件のEmpty State表示、存在しない系統番号の検索耐性、XSS攻撃文字列（`<script>`）のエスケープ、余分な空白のトリム処理、重複リセットの冪等性。
- **B8: ストレージ境界 (5 tests)**: 空APIキー入力時の初期キーフォールバック、2000文字極長キー耐性、`QuotaExceededError` 発生時のグレースフル処理、非数値バッファ入力のデフォルト化、破損JSONキャッシュの自動パージと再生成。
- **B9: APIエラー・ネットワーク境界 (6 tests)**: HTTP 401（認証失敗）フォールバック、HTTP 403（アクセス拒否）フォールバック、HTTP 429（レート制限）バックオフ、HTTP 500（サーバー障害）フォールバック、ネットワークタイムアウト/AbortSignal耐性、遅延フィールド欠落時の安全な0秒フォールバック。
- **B10: ポーリング・並行性境界 (5 tests)**: 手動更新ボタン連打（500ms内に10回）の1回デバウンス、タブ非アクティブ時のポーリング停止、アクティブ復帰時の即時同期再開、ポーリング間隔クランプ（10s〜120s）、非同期レースコンディション防止。

### Tier 3: 複数機能直交組み合わせ（16テスト）
- `T3.1`: 復路反転 ＋ 133系統フィルター ＋ 8分カスタムバッファ
- `T3.2`: オフライン起動 ＋ ダークモード ＋ 元日祝日判定 ＋ 乗り継ぎ計算
- `T3.3`: HTTP 401エラーフォールバック ＋ 自動更新 ＋ 設定モーダルでの正規キー復帰
- `T3.4`: 第1便+15分遅延 ＋ 系統絞り込み ＋ リアルタイム自動再計算
- `T3.5`: 上大岡停留所ビュー ＋ 土曜ダイヤ ＋ 64系統フィルター ＋ ダークテーマ
- `T3.6`: 負のバッファサニタイズ ＋ 復路反転 ＋ 複数代替候補抽出
- `T3.7`: ネットワーク復帰（オフライン→オンライン） ＋ 手動更新 ＋ 運行ステータス同期
- `T3.8`: 深夜23:30乗り継ぎ ＋ 24時またぎ判定 ＋ 第2区間終電後フォールバック
- `T3.9`: LocalStorageキャッシュTTL（7日）満了 ＋ 自動再取得 ＋ UI更新
- `T3.10`: 複数系統（111+64）フィルター ＋ 復路 ＋ 停留所発車リストソート
- `T3.11`: 60分長大バッファ ＋ 代替候補3便以上の抽出
- `T3.12`: Page Visibilityバックグラウンド ＋ 設定変更 ＋ フォアグラウンド即時再描画
- `T3.13`: 運休アラート表示中 ＋ 正常便カウントダウン ＋ 乗り継ぎからの運休便除外
- `T3.14`: PWAオフラインシェル ＋ モック時刻表照会 ＋ 全タブ切り替えナビゲーション
- `T3.15`: カスタムAPIキー ＋ ダークモード ＋ 振替休日の乗り継ぎ案内
- `T3.16`: 行先「根岸駅前」絞り込み ＋ 往路第2区間ピンポイント抽出

### Tier 4: 実世界アプリケーションシナリオ（9テスト）
- `T4.1`: 朝の通勤シナリオ（平日07:30 洋光台北口 → 上大岡 → 古泉、111系統＋133系統、定刻接続と後続2便提示）
- `T4.2`: 夕方の帰宅シナリオ（平日18:45 古泉 → 上大岡 → 洋光台北口、反転ルーティング、7分バッファ）
- `T4.3`: 休日家族行楽シナリオ（日曜11:00 洋光台北口 → 上大岡 → 古泉、休日ダイヤ＋64系統接続）
- `T4.4`: 突発遅延・接続逃し回復シナリオ（111系統が+18分遅延、予定していた133系統を逃し、後続の133系統へ自動リカバリー）
- `T4.5`: 初回利用ユーザーセットアップ（PWA起動、設定モーダルでAPIキーと3分バッファを設定しダークモードへ変更、即時反映）
- `T4.6`: 地下鉄トンネル内完全オフライン確認（電波圏外でPWAシェル起動、キャッシュ時刻表から直近便と乗り継ぎを確認）
- `T4.7`: 年末年始特別ダイヤ乗り継ぎ（1月2日金曜日に検索、曜日に関わらず休日ダイヤを正しく適用して案内）
- `T4.8`: 系統別路線探索 & 全停留所時刻表閲覧（64系統に絞り込み、上大岡駅前停留所の全発車時刻を時間帯別に閲覧）
- `T4.9`: デスク常時表示モニタリング & ライフサイクル（30秒ごとの自動更新、遅延推移の追跡、ウィンドウ最小化時の自動停止と復帰時同期）

---

## 5. 期待値導出根拠 (Expected Output Derivation)
本テストスイートの全テストケースは、以下の公式ドキュメントおよび仕様に基づき決定論的に導出されています：
1. **`ORIGINAL_REQUEST.md` / `REQUIREMENTS.md`**:
   - 上大岡乗り継ぎアルゴリズム: 到着予測 $T_{arr1} = T_{dep1} + travelTime_1 + delay_1$ に対し、接続可能最小時刻 $T_{min2} = T_{arr1} + Buffer$ を満たす直近の第2区間便を抽出。
   - 横浜市営バス所要時間標準値: 洋光台北口〜上大岡駅前（約15分）、上大岡駅前〜古泉（約12分）。
   - デフォルトバッファ: 5分、デフォルトポーリング: 30秒。
2. **公共交通オープンデータセンター (ODPT API v4) 仕様**:
   - `odpt:BusstopPole`, `odpt:BusRoutePattern`, `odpt:BusTimetable`, `odpt:Bus`, `odpt:BusInformation` の各JSON-LDスキーマ。
   - `odpt:operator = odpt.Operator:YokohamaMunicipal`。
3. **国民の祝日に関する法律 & 横浜市営バス運行規程**:
   - 国民の祝日、振替休日、ハッピーマンデー、春分/秋分天文計算式、年末年始（12/29〜1/3）特別休日ダイヤ適用規則。

---

## 6. 品質ステータス & 結論 (Quality Status & Conclusion)
- **テストランナー**: 正常稼働（`tests/e2e-runner.js`）
- **全132テストケース**: **100.0% 合格（132 passed / 0 failed, 87.1ms）**
- **受入準備完了 (Test Ready)**: 本E2Eテストスイートは、今後の全マイルストーン実装（M1〜M5）の回帰検証および受入テストを網羅的に担保する準備が完了しました。
