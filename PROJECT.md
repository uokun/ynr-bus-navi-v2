# Project: Transporter - Yokohama City Bus Transit Guide & Location Navigator (v2.0 Mobile Edition)

## Architecture Overview
- **Overview**: 横浜市営バス（111系統, 133系統）のODPT APIリアルタイム在線位置（`odpt:Bus`）および遅延データを解析し、スマートフォンの親指操作（Thumb Zone）に最適化されたネイティブアプリ（iOS / Android PWA）ライクなモバイルUI/UXを提供するWebアプリケーション。

## Calendar & Day Identification Specification (平日・土曜・日曜・祝日・年末年始の自動判定)
- **`calendarService.getDayDetail(date)` による高精度な日種別判定**:
  - **平日 (`weekday`)**: 月曜日〜金曜日（国民の祝日・振替休日・年末年始を除く）。`calendarType: 'Weekday'`（平日ダイヤ）。
  - **土曜日 (`saturday`)**: 土曜日（祝日を除く）。`calendarType: 'Saturday'`（土曜ダイヤ）。
  - **日曜日 (`sunday`)**: 日曜日。`calendarType: 'Holiday'`（休日ダイヤ）。
  - **祝日・特別日 (`holiday`)**:
    - 固定祝日（元日、建国記念の日、天皇誕生日、昭和の日、憲法記念日、みどりの日、こどもの日、山の日、文化の日、勤労感謝の日）。
    - ハッピーマンデー（成人の日、海の日、敬老の日、スポーツの日）。
    - 天文計算祝日（春分の日、秋分の日）。
    - 国民の休日（祝日に挟まれた平日）。
    - 振替休日（祝日が日曜と重なった場合の翌平日、GW連休振替）。
    - 年末年始特別ダイヤ期間（12月29日〜1月3日）。
  - 各種判定ヘルパー: `isWeekday(date)`, `isSaturday(date)`, `isSunday(date)`, `isHoliday(date)`, `getHolidayName(date)`。
  - UI連携: 停留所画面の時刻表ヘッダーに `📅 今日は 平日ダイヤ (水曜)` などの日種別バッジを自動表示。

## Navigation & Tab Architecture (3タブ構成 + ヘッダー設定)
- **ボトムナビゲーション (Thumb Zone 最適化)**:
  - **「乗り換え」 (`#view-transfer`)**: ダイレクト接続乗り換え案内。
  - **「停留所」 (`#view-stops`)**: 洋光台北口 / 上大岡駅前 / 古泉の停留所別発車案内 & バス接近バー。
  - **「走行位置」 (`#view-map`)**: JR東日本アプリ風 上下線複線縦型リアルタイム路線図。
- **設定画面へのアクセス**:
  - ヘッダーの ⚙️ 設定ボタンから直接「設定ビュー (`#view-settings`)」へ遷移。

## 停留所ビュー (Stop Departures View) & インライン全時刻表仕様 (Modern Inline UX)
- **サブモード切替 (「⏱ 直近発車便」 / 「📖 全時間帯 時刻表」)**:
  - 画面上部にスマートなセグメントスイッチを配置し、ワンタップで直近発車便と全時刻表グリッドをシームレスに切り替え。
  - 下部の「全時間帯時刻表を見る」ボタンを押した際も、下から競り上がるモーダルではなく、**その場に全時間帯時刻表グリッドがスムーズに展開**。
- **インライン全時間帯時刻表グリッド**:
  - 各時間帯（5時〜24時）の行、発車分チップ（系統・行先付き）、始発・終発サマリー。
  - **現在時刻ハイライト**: 現在の時間帯行をアクセントカラーと「現在」バッジで強調。
  - **ダイヤ切り替えピル**: 「平日ダイヤ」「土曜ダイヤ」「休日ダイヤ」を即座に切り替え可能。
  - **上大岡駅前**: 「12番 古泉方面」「6番 洋光台方面」ののりばピルで、時刻表も即座に完全連動。
- **洋光台北口 & 古泉 (手前5停留所 横並び接近プログレスバー)**:
  - 先発便Heroカードの直下に、**手前5つの停留所＋当停留所（計6ノード・5区間）の横並びプログレスバー**（左から右へ進行）を描画。
  - **洋光台北口 (111系統 上大岡行)**: `バイパス下 ➔ 洋光台五丁目 ➔ 洋光台駅前 ➔ 西公園前 ➔ 洋光台二丁目 ➔ 洋光台北口(当バス停)`
  - **古泉 (133系統 上大岡行)**: `坂下公園前 ➔ 滝頭 ➔ 市電保存館前 ➔ 滝頭地域ケアプラザ前 ➔ 仲之町 ➔ 古泉(当バス停)`
  - 走行中バス（🚍）アイコンがどの区間・停留所にいるかをリアルタイム位置（パーセンテージ）＋遅延バッジ（+◯分/定刻）＋パルス波紋で視覚化。
- **上大岡駅前 (`odpt.BusstopPole:YokohamaMunicipal.KamiookaStation.1046.x`)**:
  - **古泉方面 (133系統)**（12番ポール / 平日: 38便 / 土休: 22便）
  - **洋光台方面 (111系統)**（6番ポール / 平日: 69便 / 土休: 46便）
  - ※ 乗り場番号や行先等の冗長な表記を完全撤廃し、**「古泉方面」「洋光台方面」**の極限までシンプルな2分割ボタン・見出しを提供。漏れなく網羅。

## Geolocation & Nearest Stop Auto-Routing (現在地による最寄り停留所・自動ルーティング)
- **`locationService.getNearestStop(coords)` による最近接停留所判定**:
  - `navigator.geolocation` で現在地のGPS座標（緯度・経度）を取得し、Haversine球面距離計算で「洋光台北口」「上大岡駅前」「古泉」の中から最短距離の停留所を特定。
- **インテリジェント初期ルーティング**:
  - **洋光台北口が最寄りの場合**: 乗り換えタブを「洋光台北口 ➔ 古泉（往路）」を出発支点として表示。
  - **古泉が最寄りの場合**: 乗り換えタブを「古泉 ➔ 洋光台北口（復路）」を出発支点として表示。
  - **上大岡駅前が最寄りの場合**: 乗り換えページではなく、**停留所タブ（上大岡駅前）を自動選択して直接表示**。
- **「停留所」タブ遷移時の自動選択**:
  - ボトムナビで「停留所」を選択した際、現在地から最も近い停留所を自動選択して即座に発車案内を表示。手動選択時はユーザーの選択を優先維持。行先等の冗長な表記を完全撤廃し、**「古泉方面」「洋光台方面」**の極限までシンプルな2分割ボタン・見出しを提供。漏れなく網羅。

## 走行位置ビュー (Live Double-Track Route Map) 仕様 (JR東日本アプリ風 & ライト/ダーク両対応)
- **上下線2本立て（複線）縦型路線図**:
  - **ライト/ダーク両対応デザインシステム (`--dt-*`)**:
    - ライトモード時: 清潔感のあるホワイトカード・高コントラストな駅名文字・爽やかな系統別レールライン（上り: `#0284C7`, 下り: `#059669`）。
    - ダークモード時: ディープネイビー背景・ネオングローアクセント・上品なコントラスト。
  - **継ぎ目のない完全な一直線レール（Continuous Straight Line Rail）**:
    - 各停留所行を貫通する一本の連続レールライン。始発駅（`is-first`）の中心から終点駅（`is-last`）の中心まで途切れず一直線に接続。
    - 停留所ノード（ドット）は駅名と完璧に縦中央で揃い、主要停留所（上大岡駅前、洋光台北口、洋光台駅前、古泉、港南台駅前、根岸駅前）は太字＆主要タグ＆二重丸ハイライト。
  - **上部セグメントコントロール & サマリー**:
    - 「111系統」「133系統」の系統切り替えタブ（選択時に系統カラーのシャドウとハイライト）。
    - 上り/下りの運行台数サマリーバッジ（リアルタイム運行台数のパルス表示）。
  - **左側トラック (上り線: 上大岡駅前 方面)**: 上大岡行きの走行中バスアイコン（🚍）＋遅延バッジ＋上向き矢印（↑）をピルカードで表示。
  - **右側トラック (下り線: 港南台駅前 / 根岸駅前 方面)**: 港南台/根岸行きの走行中バスアイコン（🚍）＋遅延バッジ＋下向き矢印（↓）をピルカードで表示。
  - 停車中（at_stop）および区間走行中（en_route）を高精度にプロット。


## Transit Routing & Settings Specification
- **乗り換えダイレクト接続仕様 (バッファ撤廃)**:
  - 乗り換えバッファ時間（待ちマージン）を撤廃（0分）。
  - 第1区間到着時刻 $T_{arr1}$ 以降（$T_{dep2} \ge T_{arr1}$）で最も早く発車する便をダイレクトに案内。
  - 上大岡駅での乗り換え待ち時間は純粋な待機時間（$T_{dep2} - T_{arr1}$）として表示。
- **UI表示の極限シンプル化 (余計な文言・絵文字の完全排除)**:
  - 乗り換え画面の出発時刻（最上部 `06:22`）および到着時刻（`06:57`）を特大（1.32rem 極太 Bold）で拡大表示し、出発停留所（洋光台北口 / 古泉）の横に **`T-mm:ss`**（例: `T-04:12`）コロン形式カウントダウンを配置。
  - 後続候補リストから「候補2」「候補3」等の不要なバッジを完全撤廃し、**「06:27 発 ➔ 06:57 着」** の発着時刻を一番大きく太字（1.25rem Bold）で最優先表示。
  - 方向切り替えボタンをクリーンなSVG矢印アイコン付きピルボタンに刷新し、絵文字・文字化け感を完全排除。
  - 方向切り替え時の「切り替えました」トースト通知（ポップアップ）を廃止し、操作感を大幅に向上。
  - `<meta name="mobile-web-app-capable" content="yes">` に対応し、ブラウザ警告を完全排除。
  - ヘッダーのロゴアイコン（`🚍`）およびサブタイトル（「洋光台北口 ⇄ 上大岡 ⇄ 古泉」）を削除。
  - メイン画面上部の「出発 洋光台北口 ⇄ 到着 古泉」巨大ルートカードを廃止。
  - 「平常運転」ステータスバナーを平常時は非表示化（異常・遅延・未設定時のみ表示）。
  - 手動更新ボタンを排除し、毎秒自動更新（ライブタイカー）によってカウントダウンと発車済み便の次便切り替えを完全自動化。
  - 「所要時間約35分」「最速ルート」等のサマリー表記を撤廃。
  - 60分以上先の便に対する「あと5時間何分」等の過剰なカウントダウンを非表示化（直近60分以内の便のみ表示）。
  - バスの絵文字（🚌）や歩行者絵文字（🚶‍♂️）、装飾記号（★, 🕒）を排除。
  - 「何駅乗車」「何番ホームへ移動」「何番ホーム」等の冗長なテキストを全廃し、乗り換え行はシンプルに **「乗り換え約X分」** のみ表示。
  - 予定便に対する「運行予定」バッジを完全削除（走行中バスが存在する場合のみリアルタイム位置を表示）。
- **設定画面の一元化 (クイック設定モーダルの撤廃)**:
  - クイック設定モーダルを廃止し、ヘッダーの ⚙️ 設定ボタンから「設定ビュー (`#view-settings`)」へ直接遷移。
  - 設定ビューでは「データ更新間隔」「カラーテーマ」「全時間帯時刻表」「APIキー＆キャッシュ管理」を一元提供。
- **ODPT APIキーの安全管理 & ローカルストレージ運用仕様 (Web公開対応)**:
  - ソースコードから個人用APIキーを完全除去（`DEFAULT_CONSUMER_KEY = ''`）。GitHub等への公開リポジトリへの配置でもキー流出なし。
  - APIキーはブラウザの `localStorage`（キー: `transporter_api_key`）にのみ安全に保存・管理。
  - 設定画面からAPIキーを一度入力・保存すると、ODPT API（`odpt:BusTimetable` / `odpt:Bus`）への直接リクエストが開始され、リアルタイム在線位置および時刻表が取得されます。

## Data Integrity & Pure API Policy (内蔵時刻表・モック完全排除方針)
- **内蔵データの完全撤廃とAPI直接取得の徹底**:
  - 静的な内蔵時刻表データやフォールバック用モックデータを完全排除。
  - 時刻表および運行情報はすべて ODPT API より直接取得。
  - 横浜市営バスの ODPT API 仕様に準拠し、APIキー設定時に `odpt:BusTimetable?dc:title=111系統` および `dc:title=133系統` をリクエストして停留所×カレンダー別に動的インデックス化。
  - APIキーが未設定の場合、または API から時刻表が取得できなかった場合は、架空データで誤魔化さず「⚠️ 時刻表データを取得できませんでした（設定からAPIキーを入力してください）」と明確にエラー画面を表示。
  - `odpt:Bus` リアルタイムバス位置情報が取得できない場合も、架空のバスを走らせず、「運行中: 0台」「バス位置情報を取得できませんでした」と正直に表示。

## Test Verification Summary
- **Master E2E Suite (Tier 1 to Tier 5)**: `node tests/e2e-runner.js` -> **168/168 PASS (100%)**
- **5-Stop Approaching & Double-Track Map Suite**: `node tests/test-approaching-and-double-track.js` -> **58/58 PASS (100%)**
- **Bus Location Engine Suite**: `node tests/test-bus-location.js` -> **97/97 PASS (100%)**
- **UI & Data Unit Tests**: `node tests/m4-m5-unit-tests.js` -> **17/17 PASS (100%)**
- **Reviewer Deep Check**: `node tests/reviewer-deep-check.js` -> **125/125 PASS (100%)**
- **Adversarial DOM Lifecycle Test**: `node tests/adversarial-final-lifecycle-test.js` -> **21/21 PASS (100%)**
- **Tab Switching Verification Test**: `node tests/test-tab-switching-fix.js` -> **6/6 PASS (100%)**
- **Total Validated**: **492 / 492 Tests Passing (100%)**
